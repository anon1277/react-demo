// Importing createContext from React to create a new context
import { createContext } from "react";

// Creating a context named 'SubjectContentData' with a default value of "Math"
export const SubjectContentData = createContext("Math");
